@model Actor

@{
    ViewData["Title"] = "Delete Actor";
}
<div class="row text">
    <div class="col-md-8 offset-2">
        <p>
            <h1>Delete Confirmation for @Model.FullName</h1>
        </p>
        <div class="row">
            <div class="col-md-8 offset-2">

                <div class="form-group">
                    <label asp-for="ProfilePicture" class="control-label"></label><br />

                    @if (Model.ProfilePicture != null)
                    {
                        <img class="rounded-circle" src="data:image;base64,@System.Convert.ToBase64String(Model.ProfilePicture)" alt="@Model.FullName" style="max-width: 150px" />
                    }
                    else
                    {
                        <img class="rounded-circle" src="https://cdn.pixabay.com/photo/2015/10/05/22/37/blank-profile-picture-973460_960_720.png" alt="@Model.FullName" style="max-width: 150px" />
                    }
                </div>
                <div class="form-group">
                    <label asp-for="FullName" class="control-label"></label>
                    <input asp-for="FullName" class="form-control" readonly/>
                    <span asp-validation-for="FullName" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Bio" class="control-label"></label>
                    <input asp-for="Bio" class="form-control" readonly/>
                    <span asp-validation-for="Bio" class="text-danger"></span>
                </div>

                <div class="form-group">
                    <form asp-action="Delete">
                        <input type="hidden" asp-for="Id"/>
                        <input type="submit" value="Confirm" class="btn btn-danger float-right"/>
                    </form>
                    <a class="btn btn-outline-secondary" asp-action="index">View All</a>
                </div>
            </div>
        </div>
    </div>
</div>